[package]
name = "cce-color-editor"
version = "0.1.0"
edition = "2021"

[dependencies]
cce-ui = { path = "../cce-ui" }
wayland-client = { version = "0.31", features = ["system"] }
glyphon = "0.8"
calloop = "0.13.0"
tokio = { version = "1", features = ["full"] }

